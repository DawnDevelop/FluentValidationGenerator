{% for p in Properties -%} 
{% if p.Required == true -%}
{% if p.Nullable == false -%}
	RuleFor(t => t.{{ p.Name }}).NotEmpty().WithMessage("{{ p.Name }} can not be Empty");
{% else -%}
	RuleFor(t => t.{{ p.Name }});
{% endif -%}
{% endif -%}
{% endfor -%}